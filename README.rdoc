== README

---------About Email Sending---------

* To send email asynchronously by sidekiq, enviroment setup as follows, then sidekiq will monitor the scheduled jobs
    * install redis
    * bundle exec sidekiq

* Set Environment varialbe ENV['BACKGROUND_PROCESS'] when running rails application

